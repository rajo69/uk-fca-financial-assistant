# Resumability — Where We Are

**Read this at the start of every Claude Code session. Update at the end.**

## Current phase

**Phase 3 — Domain immersion (user-only reading)**

Status: ready to start. Mostly off-Claude work.

## Last session

- Date: 2026-05-19
- Session name: `phase-02-scaffolding`
- Permission posture: ran with `--dangerously-skip-permissions` (see D2.2).
  Hooks remained active; diff to be reviewed by user.
- What was done:
  - Created the directory tree: `.github/workflows/`, `configs/`, `data/`
    (raw, sources, sft/{seeds,synthetic,final}, dpo, eval), `docs/`,
    `notebooks/`, `scripts/`, `src/ukfca/` (with data, training, eval, rag,
    serving submodules), `tests/{unit,integration,fixtures}/`,
    `checkpoints/`, `tools/`
  - Created Python project files:
    - `pyproject.toml` — core deps + optional [dev, data, eval, rag, serving]
      extras (training deps deliberately Kaggle-only)
    - `uv.lock` — generated by `uv sync --extra dev`
    - `.pre-commit-config.yaml` — ruff, mypy-on-src, gitleaks, standard
      pre-commit-hooks
    - `Makefile` — install / test / lint / format / precommit / clean
  - Module skeletons in `src/ukfca/` with one-line docstrings pointing at
    the phase that will populate each.
  - `tests/unit/test_sanity.py` — trivial passing test wiring up coverage.
  - `.github/workflows/ci.yml` — ruff format check, ruff lint, mypy strict,
    pytest. Python 3.11. `astral-sh/setup-uv@v3` with cache enabled.
  - `README.md`, `docs/IMPLEMENTATION_PLAN.md` (full 13-phase plan),
    `docs/ARCHITECTURE.md` (placeholder), `docs/REGULATORY_NOTES.md`
    (placeholder for user's Phase 3 notes).
  - `.gitignore` — appended `mlruns/`, plus `checkpoints/*` and `data/raw/*`
    with `!.gitkeep` negations so dir markers stay tracked.
  - Ran `uv sync --extra dev` (34 packages installed) and
    `uv run pre-commit install`.
  - Verified: `uv run pytest tests/unit -v` → 1 passed. `uv run ruff check`
    → clean. `uv run ruff format --check` → 12 files already formatted.
    `uv run mypy src` → no issues in 8 source files.
  - Recorded D2.1 (W&B) and D2.2 (skip-permissions for this phase) in
    DECISION_LOG.md as DECIDED.
  - Added skip-permissions and gitignore-negation lessons to lessons.md.
- What's pending:
  - User diff review of the full Phase 2 changeset (six commits).
- Next action: Phase 3 — domain immersion (off-Claude reading by user).
- Last-known-good git SHA: `401ceef` (chore(gitignore): uv/build artefacts,
  mlruns, data/raw) — to be updated on push after the resumability commit.

## Next action

Phase 3 — domain immersion. This is largely NOT Claude Code work.

1. User reads:
   - FCA PS25/22 — Targeted Support policy statement
   - FCA Consumer Duty (PRIN 2A)
   - FCA AGBR — Advice/Guidance Boundary Review
   - FCA PS7/24 — Model Risk Management
   - FCA vulnerable customer guidance
   - MoneyHelper.org.uk content
   - Relevant gov.uk pages
2. User writes notes into `docs/REGULATORY_NOTES.md` in their own voice.
3. Decision D3.1 — scope (pensions only / investments only / both) — record
   in DECISION_LOG.md.
4. When ready, start a new named session `claude -n phase-04-rubric` for
   Phase 4 work.

## Blockers

None.

## Open decisions awaiting user input

See DECISION_LOG.md for any entries with status PENDING.

Currently pending:
- D3.1 — scope of project (pensions / investments / both). To be decided
  during or after Phase 3 reading.

## Phase tracker

- [x] Phase 0 — Master plan agreed (off-repo)
- [x] Phase 1 — Workflow foundations
- [x] Phase 2 — Repo scaffolding
- [ ] Phase 3 — Domain immersion (user-only reading)  *(current)*
- [ ] Phase 4 — Annotation rubric & data schema
- [ ] Phase 5 — Data ingestion (RAG sources)
- [ ] Phase 6 — SFT seed examples (user-heavy)
- [ ] Phase 7 — Synthetic expansion & review tooling
- [ ] Phase 8 — Eval test set (user-heavy)
- [ ] Phase 9 — SFT training pipeline
- [ ] Phase 10 — Eval harness & LLM-as-judge
- [ ] Phase 11 — DPO pipeline
- [ ] Phase 12 — RAG layer
- [ ] Phase 13 — Serving, deployment, README

## End-of-session checklist (Claude Code must run before stopping)

1. [ ] All staged changes committed with conventional commit messages
2. [ ] DECISION_LOG.md updated if any decisions were made
3. [ ] lessons.md updated if any mistakes were made and corrected
4. [ ] This file (RESUMABILITY.md) updated:
       - Date
       - What was done
       - Next action
       - Blockers
       - Pending decisions
5. [ ] git push (if on a feature branch or with user approval if main)
6. [ ] Print a 3-line session summary to the user
